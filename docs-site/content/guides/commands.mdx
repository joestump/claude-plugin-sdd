---
title: Commands
sidebar_label: Commands
sidebar_position: 2
---

{/* Governing: ADR-0029, SPEC-0021 REQ "Migration of commands.mdx (Step 2 — Audit and Redirect)". */}

# Commands

This page has moved.

The per-command reference now lives at **[/skills/](/skills/)** — one page per skill, grouped by lifecycle stage. Bookmarks pointing at `/guides/commands#<anchor>` are redirected automatically by `@docusaurus/plugin-client-redirects`.

- New per-skill pages: `/skills/{name}` (e.g., [`/skills/work`](/skills/work), [`/skills/audit`](/skills/audit))
- Hero-tile overview: [`/skills/`](/skills/)

This stub remains in place for at least one release cycle so external mirrors and search-engine indexes have time to catch up. Story #143 (SPEC-0021) deletes the file once the redirects are confirmed comprehensive.
